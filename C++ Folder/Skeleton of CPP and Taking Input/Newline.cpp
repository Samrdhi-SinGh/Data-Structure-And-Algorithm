#include<bits/stdc++.h> // The <bit/stdc++.h> is a library which contains all the libraries like <iostream>, <math.h>, <string> and many more. It takes a bit more time than usual libraries but the time are negligible.
using namespace std; // We use 'using namepace std;' instead of std:: because it will time taking to type std:: everytime we use a function which exists in std:: like cout, endl. It's better to write this and all the function will eventually work.

int main(){
    cout << " Hey Samrdhi!";
    cout << " Hope yoou are doing good."; // This line will be printed in the same above line.
    cout << " Have a great day ahead."<<"\n"; // '\n' will break the line and the next line will not be in same line.
    cout << " work hard." << endl; // 'endl'function is same as '\n'. Just '\n' is faster than 'endl'.
    cout << " Work smart.";
    return 0;
}