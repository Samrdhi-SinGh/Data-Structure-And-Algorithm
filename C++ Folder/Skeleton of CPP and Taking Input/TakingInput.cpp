//User Input :-

#include <bits/stdc++.h>
using namespace std;

int main(){
    int x;
    cin >> x; // 'cin' is a function in 'std' which takes the input from user.
    cout << " The value of x is :"<< x;

    return 0;
}

/* 
//Take two User Input :-

#include <bits/stdc++.h>
using namespace std;

int main(){
    int x;
    int y; // We can have both in one int like 'int x, y;' 
    cin >> x ;
    cin >> y; // We can have both in one line like 'cin >> x >> y;' 

    cout << "The value of x is : "<< x << " and y is : "<< y;

    return 0;
}*/